# StudyBridge FastAPI AI 서버 의존성
# Spring Boot 계약 API (v0.5) + 확장 API (v0.6)

# ── 웹 프레임워크 ─────────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── 환경변수 / 설정 ───────────────────────────────────────────────
python-dotenv
pydantic
pydantic-settings

# ── HTTP 클라이언트 ───────────────────────────────────────────────
requests
httpx

# ── 내부 에이전트 이벤트 버스 (ai07 프로세스 내부 전용, inproc/ipc) ──
# 멀티에이전트 SSE 이벤트 패싱 정리/순서보장/중복 fingerprint 처리용.
# manylinux 휠이 libzmq를 번들하므로 보통 시스템 패키지 불필요. (실패 시 자동 queue passthrough)
# 시스템 빌드가 필요한 환경에서는: sudo apt-get install -y libzmq3-dev
pyzmq>=25

# ── LLM 클라이언트 ────────────────────────────────────────────────
openai

# ── 검색 API ──────────────────────────────────────────────────────
tavily-python

# ── AWS S3 ────────────────────────────────────────────────────────
boto3

# ── PDF 텍스트 추출 ───────────────────────────────────────────────
PyMuPDF
pypdf

# ── Wikipedia ─────────────────────────────────────────────────────
wikipedia-api

# ── PostgreSQL + pgvector (AI RAG 전용 DB) ────────────────────────
psycopg[binary]
pgvector

# ── 임베딩 / RAG (sentence-transformers는 무겁지만 pgvector RAG에 필요) ──
sentence-transformers
numpy

# ── LangGraph / LangChain (에이전트 오케스트레이션 '흐름 제어용') ──────────
#  RAG 자체는 기존 sentence-transformers + pgvector를 그대로 사용한다.
#  LangGraph는 토론/소크라테스/RAG 판단/검증/재작성 흐름을 그래프로 구조화하는 데만 쓴다.
langgraph
langchain
langchain-core
langchain-community
langchain-text-splitters

# ── JSON / 유틸 ───────────────────────────────────────────────────
orjson
python-multipart

# ── HTTP 재시도 ───────────────────────────────────────────────────
tenacity

# ════════════════════════════════════════════════════════════════════
# 선택적 패키지 (Optional) — 필요 시 주석 해제
# ════════════════════════════════════════════════════════════════════

# TensorFlow 학습 시간 예측 모델 (실제 모델 파일이 있을 때만 설치)
# tensorflow-cpu
# TF 없으면 가중 평균 예측(fallback)으로 자동 동작함

# OCR 기반 이미지 PDF (이미지 PDF 처리 시 필요)
pytesseract
Pillow
python-docx>=1.1.2

python-multipart>=0.0.9
