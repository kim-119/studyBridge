# 목요일 시연 시나리오 — StudyBridge AI v0.4

> 시연 시간: 약 10~12분 | 개발자 설명 + 실시간 화면 시연

---

## 시연 멘트 (오프닝)

> "StudyBridge는 단순히 LLM 하나가 답하는 구조가 아닙니다.
> 자료보관함에서는 GPT 중심의 안정적인 문서 분석을 사용하고,
> 에이전트 채팅에서는 Qwen 기반의 빠른 대화와 Tavily/GPT 검증을 비동기로 결합했습니다.
> 또한 PDF 기반 RAG와 pgvector를 사용해 문서 근거를 검색하고,
> 파인튜닝 데이터는 현재 검수 단계까지 완료해 품질 기준을 확보했습니다."

---

## 시연 1: 지식수준별 답변 차이 (2분)

**질문**: "트랜잭션이란 무엇인가요?"

### 시연 순서
1. 지식수준 = **입문** → 전송
   - 기대 출력: 레고/요리 비유, 전문 용어 없음, 짧은 설명

2. 지식수준 = **박사** → 동일 질문 전송
   - 기대 출력: ACID 이론, 격리 수준, 엣지 케이스, 구조적 한계

**포인트**: "같은 질문인데 완전히 다른 깊이의 답변이 나옵니다.
입문은 비유 중심, 박사는 이론 구조 중심입니다."

---

## 시연 2: 성격별 에이전트 답변 차이 (2분)

**질문**: "인덱스를 왜 써야 하나요?"

### 시연 순서
1. 성격 = **친절_설명형**
   - 기대: 따뜻한 말투, "어렵지 않아요!" 류의 격려

2. 성격 = **비판적_분석형**
   - 기대: "무작정 인덱스를 걸면 쓰기 성능이 느려질 수 있어" 같은 직설적 지적 후 올바른 사용 기준 제시

3. 성격 = **간결_요약형**
   - 기대: 불릿 3개 + 한 줄 결론

**포인트**: "같은 에이전트인데 성격 설정만 바꿨을 뿐입니다.
GPT처럼 무색무취한 답변이 아니라 개성 있는 답변이 나옵니다."

---

## 시연 3: 에이전트 티키타카 (3분)

**질문**: "데드락이 왜 발생하고 어떻게 막나요?"
**지식수준**: 학사 | **성격**: 비판적_분석형 | **티키타카**: ON

### 기대 출력 구조
```
[자바도우미]
핵심 답변: 데드락은 두 트랜잭션이...

[자바도우미B]
그 설명만 들으면 헷갈릴 수 있어. 여기서 중요한 건...
(보완 지적)

[자바도우미C]
쉽게 말하면 두 사람이 서로 상대방 물건을 기다리는 상황이에요...
(생활 비유)

[정리]
핵심은: 데드락은 점유-대기 조건에서 발생하며...
```

**포인트**: "에이전트 세 명이 하나의 질문을 서로 다른 관점에서 대화합니다.
A가 핵심을 설명하면 B가 보완하고, C가 쉽게 요약합니다."

---

## 시연 4: 자료보관함 PDF 분석 (2분)

1. PDF 업로드 화면 → "PDF를 업로드하면..."
2. `/api/rag/ingest` 호출 → "텍스트 추출 → 청킹 → 임베딩 → pgvector 저장"
3. PDF 기반 질문: "이 논문에서 제안하는 핵심 방법론은?"
   - 기대: PDF_RAG_CONTEXT 기반 답변, "PDF에 없는 내용은 없습니다"라고 명시

**포인트**: "일반 GPT는 PDF 내용을 모릅니다.
StudyBridge는 PDF를 벡터화해서 관련 부분만 검색한 뒤 답변합니다."

---

## 시연 5: 퀴즈/로드맵 생성 (1분)

- `/api/ai/material/quiz` → "5개 사지선다 문제 + 정답 + 해설"
- `/api/ai/material/roadmap` → "단계별 학습 커리큘럼"

**포인트**: "퀴즈와 로드맵은 GPT가 전담합니다. 정확성과 안정성을 위해서입니다."

---

## 시연 6: 검증 중 표시 (1분)

1. 채팅 전송 → 즉시 답변 수신 (`"status": "pending_validation"`)
2. "GPT가 백그라운드에서 검증 중입니다..." 표시
3. `GET /api/ai/chat/validation/{job_id}` 폴링
4. completed 시 score, issues, corrected_answer 확인

**포인트**: "답변이 느려지는 이유는 검증 때문이었습니다.
v0.4에서 검증을 비동기로 분리해 사용자는 즉시 답변을 받습니다."

---

## 시연 7: 아키텍처 설명 (1분)

화이트보드 또는 AI_ARCHITECTURE_PLAN.md 다이어그램 설명:

> "이게 StudyBridge AI의 전체 구조입니다.
> React → Spring → FastAPI Orchestrator → Qwen/GPT/Tavily/Wikipedia/pgvector
> 7개 내부 모듈이 협력하고, 각 기능에 최적화된 모델을 씁니다."

---

## 시연 8: 파인튜닝 데이터 (30초)

`cleaning_report.md` 화면 공유:
- 원본 325개 → 52개 최종 통과
- 151개 중복 제거, 50개 거부
- "지금은 NOT_READY지만 파이프라인은 준비됐습니다.
  300개 이상 확보되면 바로 본학습 진입 가능합니다."

---

## 예상 Q&A

**Q: Qwen을 쓰는 이유는?**
A: 로컬 vLLM 서버로 API 비용 없이 빠른 1차 응답 생성. GPT는 검증과 안정성 요구 기능에만 선별 사용.

**Q: RAG가 없으면?**
A: 일반 질문에는 Tavily/Wikipedia + Qwen으로 답변. PDF 근거가 없으면 없다고 명시.

**Q: 파인튜닝 언제?**
A: 52개는 smoke test 수준. needs_review 71개 수정 완료 후 300개 이상이면 QLoRA 진입.

**Q: 실시간 대화가 되나요?**
A: 현재 단발성 요청-응답 구조. 히스토리 연결과 WebSocket은 다음 버전 계획.
